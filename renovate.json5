{
  extends: [
    'config:recommended',
  ],
  kubernetes: {
    enabled: true,
    managerFilePatterns: [
      '/\\.ya?ml$/',
    ],
  },
  packageRules: [
    {
      matchCategories: [
        'docker',
      ],
      enabled: true,
    },
    {
      matchUpdateTypes: ['minor', 'patch'],
      automerge: true,
      automergeType: 'pr',
      minimumReleaseAge: '3 days',
      matchConfidence: ['high', 'very high'],
    },
  ],
}
