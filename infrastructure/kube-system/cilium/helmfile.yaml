repositories:
  - name: cilium
    url: https://helm.cilium.io

releases:
  - name: cilium
    namespace: kube-system
    chart: cilium/cilium
    version: 1.19.3
    values:
      - values.yaml
  - name: cilium-resources
    namespace: kube-system
    chart: ./resources
