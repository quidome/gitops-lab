apiVersion: v1
kind: Secret
metadata:
  name: renovate-secrets
  namespace: gitops
type: Opaque
stringData:
  # Forgejo/Codeberg personal access token with repo read/write scope
  RENOVATE_TOKEN: "codeberg_pat_here"

  # Optional: improves GitHub changelog/release note fetching
  RENOVATE_GITHUB_TOKEN: "github_pat_here"
