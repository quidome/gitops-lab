apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.app.encryptionKeySecretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pocket-id.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.app.encryptionKeySecretKey }}: {{ .Values.app.encryptionKey | quote }}
