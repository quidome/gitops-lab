repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

---
releases:
  - name: demo-nginx
    namespace: demo
    chart: bitnami/nginx
    version: "18.0.0"
    values:
      - service:
          type: ClusterIP
          port: 80
