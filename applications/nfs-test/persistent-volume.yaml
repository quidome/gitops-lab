apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-demo-pv
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: "/mnt/datapool/volatile/nfs-demo"
    server: "172.16.40.64"
    readOnly: false
