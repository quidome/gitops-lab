repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginx
    namespace: onzin
    chart: bitnami/nginx
    version: 18.1.0
    values:
      - values.yaml
