apiVersion: v1
kind: Secret
metadata:
  name: {{ include "saic-mqtt-gateway.fullname" . }}
  labels:
    {{- include "saic-mqtt-gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
