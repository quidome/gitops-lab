controllers:
  main:
    containers:
      main:
        image:
          repository: saicismartapi/saic-python-mqtt-gateway
          tag: "0.9.8"
        env:
          MQTT_URI: tcp://mosquitto.home-automation.svc.cluster.local:1883
        envFrom:
          - secretRef:
              name: saic-mqtt-gateway
