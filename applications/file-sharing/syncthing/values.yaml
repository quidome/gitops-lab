strategy:
  type: Recreate

resources:
  requests:
    cpu: 10m
    memory: 128Mi
  limits:
    memory: 128Mi

persistence:
  data:
    enabled: true
    mountPath: /var/syncthing
    accessMode: ReadWriteOnce
    size: 20Gi
    storageClass: truenas-nfs
