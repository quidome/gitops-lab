apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: infrastructure-components
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  generators:
    - git:
        repoURL: git@github.com:quidome/gitops-lab.git
        revision: main
        directories:
          - path: infrastructure-legacy/networking/*
          - path: infrastructure-legacy/storage/*
          - path: infrastructure-legacy/controllers/*
          - path: infrastructure-legacy/monitoring/*
  template:
    metadata:
      name: "{{path.basename}}"
      labels:
        type: infrastructure
    spec:
      project: default
      ignoreDifferences:
        - kind: Secret
          jsonPointers:
            - /data/ca.crt
            - /data/ca.key
            - /data/tls.crt
            - /data/tls.key
      source:
        plugin:
          name: kustomize-build-with-helm
        repoURL: git@github.com:quidome/gitops-lab.git
        targetRevision: main
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
      syncPolicy:
        automated:
          prune: false
          selfHeal: false
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
          - ApplyOutOfSyncOnly=true
          - Replace=false
          - IgnoreMissingTemplate=true
